# Backend of To-do List Schedule

## Comando para iniciar o servidor dev

```shell
uvicorn myapi:app --reload
```

## Gerar o arquivo `requirements.txt`

```shell
pipreqs . --force --encoding=utf-8
```
